"""Pure-Python helpers for the Insert Support Fins command (no adsk imports).

The fin geometry itself comes from the website's engine (see engine_host.py);
this package only reshapes its output for Fusion.
"""
